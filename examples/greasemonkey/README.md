# greasemonkey
Generate a greasemonkey userscript that overrides every `RTCPeerConnection`.

## Instructions

### Run greasemonkey
Execute `go run . -origin http://localhost:8080`

The program prints a Greasemonkey or Tampermonkey userscript to stdout.
